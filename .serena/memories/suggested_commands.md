Build: `./gradlew assembleDebug`, `./gradlew assembleRelease`, `./gradlew bundleRelease`. Tests: `./gradlew test`, `./gradlew test --tests "dev.chungjungsoo.gptmobile.ExampleUnitTest"`, `./gradlew connectedAndroidTest`. Clean build: `./gradlew clean assembleDebug`. Single compile verification often used here: `./gradlew :app:compileDebugKotlin`. Search with `rg`, inspect git with `git status` and `git diff`.