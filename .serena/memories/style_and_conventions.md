Follow AGENTS.md: Kotlin/Android codebase, 4-space indentation, ktlint/android_studio style, one primary class per file, MVVM patterns with StateFlow and sealed state classes, Hilt injection, Compose patterns like collectAsStateWithLifecycle and hiltViewModel(). Naming uses PascalCase for classes/composables, camelCase for functions/vars, SCREAMING_SNAKE_CASE for constants, `_backingField` for private mutable state.